<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home page</title>
  <style>
      *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
background-color: #0046FF;
            display: flex;
            /* justify-content: center; */
        }
        .main{
align-self: flex-start;
margin-left: 10%;
        }
        h1{
            color:bisque;
            font-weight: 800;
            text-shadow: .5px .5px 8px black;
            margin: 5px;
        }
        h1::first-letter{
            color: red;
        }
        .post{
            background-color: #73C8D2;
            width: 25rem;
            height: max-content;
            border: 2px solid black;
            border-radius: 10px;
            padding: 10px;
            margin-bottom: 10px;
            box-shadow: 2px 2px 10px black;
        }
        .post h3{
            margin-bottom: 2.5px;
        }
        .post h3 span{
            color: rgb(25, 95, 25);
            text-shadow:  0.5px 0.5px black;
            text-decoration: underline;
            text-transform: capitalize;
            font-style: italic;
        }
        .button{
            display: flex;
            /* justify-content: space-around; */
            align-items: center;
            gap: 10px;
            width: 100%;
            height: 10%;
            /* background-color: #F5F1DC; */
        }
        .button button{
            border-radius: 5px;
            padding: 1.25px;
            background-color: #ff91136d;
            color: green;
            font-size: larger;
            min-width: max-content;
            align-self: flex-start;
        }
        a{
            color: bisque;
            font-weight: 800;
            font-size: larger;
            text-shadow: .5px .5px 5px black;
        }
  </style>
</head>
<body>
    <div class="main">

        <h1>Quora</h1>
         <%   for (let i = 0; i < posts.length; i++) { %>
        <div class="post">
            <h3>username :@ <span><%= posts[i].username %></span> </h3>
            <h3>id : <span> <%= posts[i].id %></span></h3>
            
            <h3>class :   <span> <%= posts[i].department %></span></h3>
            <h3>skills :   <span>  <%= posts[i].skills %></span></h3>
            <div class="button">
                <button>
                    <a href="/post/<%=posts[i].id  %>/edit">edit</a>
                </button>
                <form action="/post/<%= posts[i].id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit">delete</button>
                </form>
            </div>
        </div>
                <% } %>
                <a href="/new">CREATE NEW POST </a>
    </div>
    <script>
      
    </script>
</body>
</html>